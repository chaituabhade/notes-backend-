<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="style.css">
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-4 sm:p-10">
        <div class="w-full flex justify-end gap-4">
            <a href="/pricing" class="block w-20 h-9 py-1   text-center rounded-md text-white font-medium bg-gradient-to-r from-blue-500 to-purple-500 hover:from-blue-600 hover:to-purple-600">
                Pricing
              </a>
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <a href="/profile/upload"><img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt=""></a>
            </div>
            <h3 class="text-2xl sm:text-3xl"><span class="text-zinc-300">HELLO</span>, <%= user.username %> 👋</h3>
        </div>
        
        <h5 class="mb-8 mt-3">You can create notes</h5>
        <form action="/post" method="post">

            <textarea placeholder="What's on your mind?" class="block p-3 mb-3 outline-none w-full sm:w-1/2 md:w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-lg" name="content" id=""></textarea>
            <input class="px-5 w-full sm:w-32 py-2 bg-blue-500 rounded-lg ml-0 sm:ml-3 block" type="submit" value="Create">
        </form>
        <div class="posts mt-10 sm:mt-20">
            <h3 class="text-zinc-400">Your Notes</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post w-full sm:w-2/3 md:w-1/2 lg:w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                        <h4 class="text-zinc-300 mb-2"><%= user.username %></h4>
                        <p class="text-sm tracking-tighter"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length%> likes</small>
                        <div class="btns flex gap-4 mt-5">
                            <a class="text-blue-500" href="/likes/<%= post._id %>">Like</a>
                            <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
                            <a class="text-red-500" href="/delete/<%= post._id %>">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>




    
    
   
</body>

</html>



